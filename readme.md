# Heart Disease Prediction System

This project is a Machine Learning-based system that predicts whether a person has heart disease or not using medical input features.

## 📌 Project Overview
The model is trained using **Logistic Regression** and is designed for educational and learning purposes.

## 🧠 Technologies Used
- Python
- Scikit-learn
- Pandas
- NumPy

## 📂 Files Description
- `app.py` – Main application file
- `Logistic Regression.pkl` – Trained ML model
- `scaler.pkl` – Feature scaling file
- `heart_columns.pkl` – Model input columns

## ⚙️ How to Run
1. Install required libraries:
   ```bash
   pip install pandas numpy scikit-learn
